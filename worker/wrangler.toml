name = "kb-api"
main = "index.js"
compatibility_date = "2025-01-01"
workers_dev = true

[vars]
OWNER = "syingwang1-dotcom"
REPO = "knowledge-base"
LLM_BASE = "https://api.deepseek.com/chat/completions"
LLM_MODEL = "deepseek-chat"

# 敏感项用 wrangler secret put 设置(不写进本文件):
#   ADD_SECRET       投喂口令(自选)
#   GITHUB_TOKEN     仅限 knowledge-base 仓库的 fine-grained PAT(Contents 读写)
#   DEEPSEEK_API_KEY DeepSeek API key
